{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "proposal-api-build",
            "command": "dotnet build",
            "type": "shell",
            "problemMatcher": "$msCompile",
            "options": {
                "cwd": "${workspaceFolder}/servers/Proposal/Proposal.Api"
            }
        },
        {
            "label": "workflow-api-build",
            "command": "dotnet build",
            "type": "shell",
            "problemMatcher": "$msCompile",
            "options": {
                "cwd": "${workspaceFolder}/servers/Workflow/Workflow.Api"
            }
        }
    ]
}